import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'VocabMaster - AI-Powered Vocabulary Learning',
  description: 'Master new vocabulary with AI-generated contextual questions and spaced repetition',
  keywords: 'vocabulary, learning, spaced repetition, AI, language learning',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
